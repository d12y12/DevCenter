gitmirror:
    build: 
      context: ./servers/GitMirror
      dockerfile: Dockerfile
    image: ${server_name}
    container_name: ${server_name}
    volumes:
      - './servers/GitMirror/apps:/apps'
    networks:
      - default
