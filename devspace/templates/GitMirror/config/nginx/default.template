server {
  listen 8080;
  server_name localhost;
  
  location / {
    root  /var/www/html;
    index index.html;
  }  
 
  location ~ ^/(images|javascript|js|css|media|static|statics)/ {
    root   /var/www;
    expires 30d;
  }
${nginx_service}

}

